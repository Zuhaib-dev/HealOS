// ============================================
// HealOS Client — Zustand Store
// ============================================
// Add stores here as features are built:
// - auth.store.ts      (user session, auth state)
// - ui.store.ts        (sidebar, modals, theme)
// - notification.store.ts (toast notifications, alerts)
export {};
